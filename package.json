{
  "name": "einfuehrung-react",
  "version": "1.0.0",
  "description": "Initiale Übung für React",
  "main": "src/app.js",
  "scripts": {
    "start": "npx servor ./public --reload",
    "compile": "babel src --out-dir public/lib",
    "compile:watch": "babel src --out-dir public/lib --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Christoph Chmela",
  "license": "MIT",
  "dependencies": {
    "@babel/cli": "^7.11.6",
    "@babel/core": "^7.11.6",
    "@babel/preset-react": "^7.10.4"
  }
}
